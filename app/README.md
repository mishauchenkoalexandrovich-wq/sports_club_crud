# Sports Club CRUD
Призначення: CRUD-операції над сутностями БД (PDO + prepared statements).
Структура: public/ (роутінг), src/ (логіка/DAO), views/ (шаблони), .nginx/, docker-compose.yml.
Запуск: docker compose up -d (після налаштування).
